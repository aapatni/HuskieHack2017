<editMenu>
  <h1 class="title light-text">Edit Menu</h1>
  <script>
    import pouchdb from '../../../pouchdb'

    this.on('mount', async () => {
      const sortablejs = await import('sortablejs')

    })
  </script>
</editMenu>
